import { AdminPlaceholderPage } from "@/components/admin-placeholder-page";

export const metadata = { title: "Bài cộng đồng — Admin" };

export default function AdminCommunityPage() {
  return <AdminPlaceholderPage module="community" />;
}
