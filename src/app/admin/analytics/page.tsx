import { AdminPlaceholderPage } from "@/components/admin-placeholder-page";

export const metadata = { title: "Analytics — Admin" };

export default function AdminAnalyticsPage() {
  return <AdminPlaceholderPage module="analytics" />;
}
