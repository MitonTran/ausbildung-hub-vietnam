import { AdminPlaceholderPage } from "@/components/admin-placeholder-page";

export const metadata = { title: "Cài đặt — Admin" };

export default function AdminSettingsPage() {
  return <AdminPlaceholderPage module="settings" />;
}
