import { AdminPlaceholderPage } from "@/components/admin-placeholder-page";

export const metadata = { title: "Doanh nghiệp — Admin" };

export default function AdminCompaniesPage() {
  return <AdminPlaceholderPage module="companies" />;
}
