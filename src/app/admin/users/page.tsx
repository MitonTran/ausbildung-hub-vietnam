import { AdminPlaceholderPage } from "@/components/admin-placeholder-page";

export const metadata = { title: "Người dùng — Admin" };

export default function AdminUsersPage() {
  return <AdminPlaceholderPage module="users" />;
}
